import React from 'react';

const LoginPage = (props) => {
    return (
        <div className="login-container">
            <h3>login</h3>
            <input type="text" />
        </div>
    );
}

export default LoginPage;